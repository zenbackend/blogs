---
import { Image } from "astro:assets";
import config from "@/config/config.json";
const { src } = Astro.props;
const {
   title,
}: {
  title: string;
} = config.site;
---

<a href="/" class="navbar-brand block text-gray-100">
  
     <h5 class="text-gray-100 ">{title}</h5> 
    
</a>
